apiVersion: v1
kind: Service
metadata:
  name: notification-api
  labels:
    app: notification-api
spec:
  type: ClusterIP
  selector:
    app: notification-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
